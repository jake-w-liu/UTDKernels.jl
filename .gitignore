# figs
textbook_figs
.DS_Store

# Root package manifests are local resolver output. The dedicated docs,
# examples, paper, and validation environments keep their own manifests.
/Manifest.toml
/Manifest-v*.toml

# generated data CSVs (regenerated by the scripts; not tracked)
paper/data/*.csv
validation/data/*.csv
validation/figs/*
!validation/figs/.gitkeep
